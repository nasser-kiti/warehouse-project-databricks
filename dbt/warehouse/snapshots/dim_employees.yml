snapshots:
  - name: dim_employees
    relation: ref('eph_employees')
    config:
      schema: gold
      database: walmart
      unique_key: employee_id
      strategy: timestamp
      updated_at: employee_updated_timestamp # Use the timestamp column from the source table to determine when a record has changed.
      dbt_valid_to_current: "to_date('9999-12-31')" # Specifies that current records should have `dbt_valid_to` set to `'9999-12-31'` instead of `NULL`.